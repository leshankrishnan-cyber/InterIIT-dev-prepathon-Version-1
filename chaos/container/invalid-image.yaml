apiVersion: apps/v1
kind: Deployment
metadata:
  name: broken-api
  namespace: rca
spec:
  replicas: 1
  selector:
    matchLabels:
      app: broken-api
  template:
    metadata:
      labels:
        app: broken-api
    spec:
      containers:
        - name: api
          image: nonexistent-image:999
