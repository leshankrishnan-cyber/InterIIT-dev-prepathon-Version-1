apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: postgres-kill
  namespace: rca
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - rca
    labelSelectors:
      app: postgres
